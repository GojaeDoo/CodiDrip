export interface Profile {
  id: string;
  name: string;
  height: number;
  weight: number;
  image_url: string;
}

export interface DripPostPresenterProps {
  imageUrl: string;
  name: string;
  height: number;
  weight: number;
}
